using UnityEngine;

public class PlantGrowth : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // instantiate prefab on top of a plant
    // change it later to plant with tomatoes some time later

    // on cut function
    // on harvest function
}
